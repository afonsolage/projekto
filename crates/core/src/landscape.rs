pub const HORIZONTAL_RADIUS: usize = 0;
pub const HORIZONTAL_SIZE: usize = (HORIZONTAL_RADIUS * 2) + 1; // Includes the center one

// pub fn is_within_bounds(local: IVec3) -> bool {
//     math::is_within_cubic_bounds(local, BEGIN, END)
// }
