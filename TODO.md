TODO list generated by AI from a research about better chunk architecture

## 🎯 Milestone 0 – House-Keeping (1 day)

| Task | Tip                                                                                                                                                                   |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M0.1 | Create `ROADMAP.md` and link it in the main `README.md` so the list is always one click away.                                                                         |
| M0.2 | Add `sled = "0.34"` and `zstd = "0.13"` to `Cargo.toml` in the **server** crate (we will use them for SC persistence).                                                |
| M0.3 | Move the current `Chunk`/`ChunkStorage` code into a new module `src/world/chunk_legacy.rs`; mark everything `#[deprecated]` so the compiler warns you while you port. |


## 🧱 Milestone 1 – Sub-Chunk Data Model (3–4 days)

| Task | Tip                                                                                                                               |
| ---- | --------------------------------------------------------------------------------------------------------------------------------- |
| M1.1 | Define `SubChunkKey(IVec3)` and `SubChunk` component (see earlier answer) in `src/world/sc.rs`.                                   |
| M1.2 | Implement `CompressedBlocks` enum (`Single`, `Bits2`, `Bits8`) backed by a tinyvec + small-vec so you don’t allocate on empty SC. |
| M1.3 | Write **round-trip unit tests**: random 16³ block field → compress → decompress → assert identical to original.                   |
| M1.4 | Add `MeshStamp` component (`data_hash: u64, lod: u8`) in `src/render/mesh_stamp.rs`.                                              |
| M1.5 | Add **(de)serialisation** with `bincode` + `zstd` level 3; benchmark 1000 compressions to be sure it’s < 0.1 ms per SC.           |


## 🗃️ Milestone 2 – Persistence Layer (2–3 days)

| Task | Tip                                                                                                                       |
| ---- | ------------------------------------------------------------------------------------------------------------------------- |
| M2.1 | Create a **resource** `ScStore { db: sled::Db }` in the **server** crate; open `world.sled` on startup.                   |
| M2.2 | Implement `fn load_sc(key: SubChunkKey) -> Option<CompressedBlocks>` and `fn save_sc(key, &CompressedBlocks)`.            |
| M2.3 | Add **async** wrapper using `bevy_tasks::IoTaskPool` so disk I/O never stalls the main schedule.                          |
| M2.4 | Add a **cache layer** (`LruCache<SubChunkKey, CompressedBlocks>` with 512-entry cap) to avoid hitting the DB every frame. |


## 🧩 Milestone 3 – ECS Integration (3 days)

| Task | Tip                                                                                                                        |
| ---- | -------------------------------------------------------------------------------------------------------------------------- |
| M3.1 | Replace the old `Chunk` query sets with `Query<(&SubChunkKey, &SubChunk, Option<&MeshStamp>)>`.                            |
| M3.2 | Add a **spawner system** that watches player position every 5 ticks and spawns/despawns SC entities within a 16-SC radius. |
| M3.3 | Add a **despawn hook** that writes dirty SCs to disk on removal (`ComponentHooks::on_remove`).                             |


## 🏗️ Milestone 4 – Mesh Pipeline (5–7 days)

| Task | Tip                                                                                                              |
| ---- | ---------------------------------------------------------------------------------------------------------------- |
| M4.1 | Write a **meshing system** that iterates only dirty SCs, decompresses into a temporary SoA, and produces `Mesh`. |
| M4.2 | Cache the generated `Mesh` on disk under `cache/meshes/{key.x}_{key.y}_{key.z}.lod0.mesh.zst`.                   |
| M4.3 | Add **LOD meshes** (edge-collapse on 8³ and 4³ blocks) and store them under `.lod1`, `.lod2`.                    |
| M4.4 | Add **greedy meshing** if you currently use naive cubical meshing; it drops vertex count ~8×.                    |
| M4.5 | Add **face-merging** for same-kind blocks to reduce draw calls.                                                  |


## 🗂️ Little Cheat-Sheet

| File / Module               | Purpose                                       |
| --------------------------- | --------------------------------------------- |
| `src/world/sc.rs`           | `SubChunkKey`, `SubChunk`, `CompressedBlocks` |
| `src/world/sc_store.rs`     | `ScStore` resource (sled wrapper)             |
| `src/systems/sc_spawner.rs` | spawns/despawns SC entities                   |
| `src/systems/meshing.rs`    | builds & caches meshes                        |
| `cache/meshes/`             | disk cache for meshes                         |
| `world.sled`                | single-file DB for SC block data              |

